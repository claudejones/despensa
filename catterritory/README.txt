Cat Territory V23

Moves now count cat-placement decisions only (place/reposition/swap/remove). Manual X and Auto-X markings do not count as moves. V22 saves migrate automatically.

V25 removes the duplicate bottom Continue CTA from the solved-puzzle view.
